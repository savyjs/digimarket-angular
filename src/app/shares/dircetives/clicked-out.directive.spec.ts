import { ClickedOutDirective } from './clicked-out.directive';

describe('ClickedOutDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedOutDirective();
    expect(directive).toBeTruthy();
  });
});
